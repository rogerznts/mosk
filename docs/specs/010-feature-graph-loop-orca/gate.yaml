schema: 1
story: "010"
story_title: "Operação em loops e grafos + Orca como atuador único"
gate: FAIL
quality_score: 10
status_reason: >-
  O smoke da camada nativa expôs dois defeitos funcionais no caminho central da
  US1: `await` não sobrevive ao NDJSON de keepalive do `check --wait`, e
  `worker-start` injeta o prompt sem submetê-lo — o worker fica parado com 0
  tokens. Nenhum dos dois apareceria por leitura de código; ambos apareceram na
  primeira execução real. Os 33 FRs seguem implementados e a validação
  automatizável limpa, mas a supervisão de worker não funciona ponta a ponta.
reviewer: "Joaquim (mosk-qa)"
updated: "2026-08-05T08:15:00Z"

# Série do score: 40 → 30 → 10. A queda não é regressão do código — é
# conhecimento adquirido. Cada volta executou mais verificação e encontrou mais.
# É o caso em que ler a série isolada engana: o trabalho não piorou, a medição
# melhorou.

waiver: { active: false }

top_issues:
  - id: "QA-010-007"
    severity: high
    finding: >-
      `await` está quebrado. O `orca orchestration check --wait` emite NDJSON
      enquanto espera — uma linha `{"_keepalive":true,"_heartbeat":true,...}` a
      cada ~15s — e só depois o envelope real. O `_orca_json` captura a saída
      inteira numa string e aplica `_json_ok` sobre ela; multi-linha não é JSON
      válido, então declara falha mesmo quando o comando funcionou. Consequência:
      toda espera por `worker_done`/`escalation`/`question` falha, e o
      `delivery-id` volta vazio — o que torna o `--ack` (FR-007) inalcançável na
      prática, mesmo estando implementado.
    suggested_action: >-
      Tratar a saída de `check --wait` como NDJSON: descartar linhas com
      `_keepalive`/`_heartbeat` e validar apenas o último envelope. Cobrir com
      fixture no selftest — o formato é estável e testável offline.
  - id: "QA-010-008"
    severity: high
    finding: >-
      `worker-start` cria o terminal e o dispatch, mas o prompt chega ao buffer
      de input da TUI **sem ser submetido**: o worker ficou `running` com `0
      tokens` por minutos, com o texto visível e não processado, e a task presa
      em `dispatched`. É a mesma classe de falha que a spec 009 corrigiu para o
      `send` (entrega sem prova), agora no caminho composto — que o ADR-0013 e a
      T013 adotaram como **preferido** justamente por ser o recomendado na
      documentação.
    suggested_action: >-
      Verificar entrega após o worker-start, reusando o predicado forte do `send`
      (releitura até a sonda aparecer). Se o caminho composto não submeter de
      forma confiável, reavaliar a decisão de preferi-lo ao par
      `spawn` + `send` com confirmação, que já tem prova de entrega.
  - id: "QA-010-001"
    severity: high
    finding: >-
      Critérios de sucesso ainda sem verificação: SC-001 (ganho de tempo),
      SC-002 (zero reprocessamento), SC-003 (ask atendido), SC-004 (AC não
      atendido reprovado) e SC-007 (contador intacto). SC-006 foi fechado (4 de 4
      degradações sem erro fatal), SC-008 comprovado, SC-005 parcial (Ambiente A
      real, B simulado, C não executado). Os cinco abertos dependem de um worker
      que conclua — bloqueados por QA-010-007 e QA-010-008.
    suggested_action: >-
      Repetir o smoke depois de corrigir 007 e 008; os cinco SCs caem juntos.
  - id: "QA-010-006"
    severity: medium
    finding: >-
      `resolve_orca_cmd` aceita `$ORCA_CLI_COMMAND` sem verificar se o caminho
      existe (orca.sh:112-115). Com valor quebrado, o `check` falha adiante e
      reporta `runtime-unavailable` — o usuário lê "abra ou reinicie o app"
      quando a causa é caminho inexistente. O caminho comum (binário fora do
      PATH) diagnostica corretamente. Contraria o FR-005.
    suggested_action: >-
      Validar que o primeiro elemento é executável; se não for, reportar
      `orca-not-found` com o caminho tentado.
  - id: "QA-010-003"
    severity: medium
    finding: >-
      Gate emitido pelo mesmo contexto que implementou a spec — a condição que a
      US2 define como não-confiável. Compensado com verificação mecânica contra o
      disco e, nesta volta, com execução real; mas o julgamento de severidade
      permanece do autor.
    suggested_action: "Re-emitir de contexto limpo (subagente ou sessão nova)."
  - id: "QA-010-004"
    severity: medium
    finding: >-
      Espelho local `.claude/mosk/` defasado do template em 20+ arquivos que
      nunca recebeu e 4 legados que o template já removeu. Pré-existente, agora
      conhecido; afeta a fidelidade de qualquer teste feito neste workspace.
    suggested_action: "Item próprio para reconciliar o espelho por inteiro."
  - id: "QA-010-005"
    severity: low
    finding: >-
      As validações criaram estado no runtime do usuário: Run
      5ef64d40 (órfã, vazia), Run run_6c78a1dc9f1f e task task_78b4bd5ab9ec
      (marcada `failed`; worker encerrado com `worker-stop`).
    suggested_action: "Remover manualmente se incomodar; nada ficou em execução."

resolved_since_previous_gate:
  - id: "QA-010-002"
    note: >-
      "Camada nativa nunca exercitada" — resolvido: foi exercitada nesta volta.
      A execução confirmou `run`, `task-create` (devolvendo id de task real, o
      que valida a correção do `_id_from_json`), `task-list` com provenance,
      `worker-start` criando dispatch, `worker-read` com transcript tipado, e
      `worker-stop`. E produziu QA-010-007 e QA-010-008.

evidence:
  frs_verified: 33
  frs_total: 33
  frs_failing_in_practice: 2      # FR-007 (inalcançável) e FR-011 (não submete)
  success_criteria_verified: 2    # SC-006, SC-008; SC-005 parcial
  success_criteria_total: 8
  automated_checks:
    selftest_orca_driver: "44 asserções ok"
    lint_graph: "clean"
    audit_docs_paths: "clean (R1..R5)"
  tasks_delivered: 34
  tasks_total: 35
  checklist_matriz:
    executados: "1,2,3,17,18,20 (real) · 9,10,11 (simulado) · 19 (reprovou) · 6 parcial"
    nao_executados: "4,5,7,8,12-16,21,22"
    smoke_camada_nativa: >-
      run → task-create → worker-start → await → worker-read → worker-stop.
      Quebrou no `await`; o worker nunca processou o prompt.

nfr_validation:
  security: { status: PASS, notes: "Sem superfície nova; a remoção do Herdr reduz dependência externa." }
  reliability: { status: FAIL, notes: "A supervisão de worker não funciona ponta a ponta: await falha e o prompt não é submetido." }
  maintainability: { status: PASS, notes: "Uma camada a menos no atuador; regras em ADR e rules; selftest de 24 para 44 asserções." }
  performance: { status: CONCERNS, notes: "SC-001 não medido — nenhuma onda executou." }

risk_summary:
  totals: { critical: 0, high: 3, medium: 3, low: 1 }
  recommendations:
    must_fix:
      - "QA-010-007: tratar NDJSON no await (descartar keepalive, validar o último envelope)."
      - "QA-010-008: confirmar entrega após worker-start, ou reavaliar o caminho composto como preferido."
      - "Repetir o smoke e fechar SC-001/002/003/004/007."
    monitor:
      - "QA-010-006: diagnóstico de $ORCA_CLI_COMMAND quebrado."
      - "Re-emitir o gate de contexto limpo."
      - "Reconciliar o espelho local."
